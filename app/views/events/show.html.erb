<p id="notice"><%= notice %></p>

<p> 
  <strong>Title:</strong>
  <%= @event.title %>
</p>

<p>
  <strong>Description:</strong>
  <%= @event.description %>
</p>

<p>
  <strong>Datetime:</strong>
  <%= @event.datetime %>
</p>

<p>
    <% @event.tags.each do |tag| %>
      <%= link_to tag, tag_events_path(tag.name) %>
    <% end %>
</p>

<%= link_to 'Edit', edit_event_path(@event) %> |
<%= link_to 'Back', events_path %>
