<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "wedding-planner" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>

  <body>
  <div class="row">
    <% if notice %>
      <p class="notice alert-box success large-6 colums large-centered"><%= notice %></p>
    <% end %>
    <% if alert %>
      <p class="alert alert-box alert large-6 colums large-centered"><%= alert %></p>
    <% end %>
    </div>
    <%= yield %>
    <%= javascript_include_tag "application" %>
  </body>
</html>
